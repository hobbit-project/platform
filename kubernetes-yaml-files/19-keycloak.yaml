apiVersion: v1
kind: Service
metadata:
  name: hobbit-keycloak
  labels:
    app: hobbit-keycloak
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31000
  selector:
    app: hobbit-keycloak
  type: NodePort
