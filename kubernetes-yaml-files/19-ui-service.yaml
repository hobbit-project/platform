apiVersion: v1
kind: Service
metadata:
  name: hobbit-gui
spec:
  selector:
    app: hobbit-gui
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31000
  type: NodePort
